import 'package:flutter/material.dart';

class FlutterMaticAbout extends StatelessWidget {
  static String id = 'fluttermatic_about';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('About'),
      ),
    );
  }
}
