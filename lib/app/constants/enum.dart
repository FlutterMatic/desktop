enum WelcomeTab {
  gettingStarted,
  installFlutter,
  installEditor,
  installGit,
  installJava,
  restart,
}

enum EditorType {
  vscode,
  androidStudio,
  none,
}

enum Progress {
  none,
  started,
  checking,
  downloading,
  extracting,
  done,
  failed,
  found,
}

enum Java {
  jdk,
  jre,
}
