enum ApplicationCheckType {
  ADB_CHECK,
  ANDROID_STUDIO_CHECK,
  FLUTTER_CHECK,
  GIT_CHECK,
  JAVA_CHECK,
  VSC_CHECK,
  DONE,
}

enum ProgressType {
  CHECKING,
  DOWNLOADING,
  EXTRACTING,
}

enum WelcomeTab {
  GETTING_STARTED,
  INSTALL_FLUTTER,
  INSTALL_EDITOR,
  INSTALL_GIT,
  INSTALL_JAVA,
  RESTART,
}

enum EditorType {
  VSCODE,
  ANDROID_STUDIO,
  BOTH,
}

enum Progress {
  STARTED,
  CHECKING,
  DOWNLOADING,
  EXTRACTING,
  DONE,
  FAILED,
}
