enum ApplicationCheckType {
  ADB_CHECK,
  ANDROID_STUDIO_CHECK,
  FLUTTER_CHECK,
  GIT_CHECK,
  JAVA_CHECK,
  VSC_CHECK,
  DONE,
}

enum ProgressType {
  CHECKING,
  DOWNLOADING,
  EXTRACTING,
}

enum WelcomeTab {
  Getting_Started,
  Install_Flutter,
  Install_Editor,
  Install_Git,
  Install_Java,
  Restart,
}

enum EditorType {
  VS_Code,
  Android_Studio,
  Both,
}